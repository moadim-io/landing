---
"landing": minor
---

Add a site footer (`app/layout.tsx`) with a copyright line and GitHub/crates.io links, rendered as a top-level `<footer>` below `<main>`. The site previously exposed only the `banner` and `main` landmarks with no `contentinfo` region, no copyright/attribution line, and no persistent secondary navigation at the bottom of the page (#28).
