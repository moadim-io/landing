---
"landing": patch
---

Mention the `tmux` runtime dependency in the "Which operating systems are supported?" FAQ answer. The hero install card already warns that `tmux` (alongside `crontab`) is required or "the install succeeds but nothing runs," but the FAQ — whose whole purpose is to answer pre-install objections — silently dropped it, leaving readers who skip the hero's fine print with an incomplete picture of what's needed.
