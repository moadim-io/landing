---
"landing": patch
---

Document `app/version.json/route.ts` (the build-provenance API route) in README.md's and AGENTS.md's project-structure listings — it shipped with tests and is referenced elsewhere in the README ("Confirming what's live"), but was never added to either file's own file-by-file breakdown, leaving it undiscoverable for anyone skimming those lists the way every other route already is.
