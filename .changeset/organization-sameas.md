---
"landing": patch
---

Add `sameAs` to the `Organization` JSON-LD node in `app/layout.tsx`, linking it to the `https://github.com/moadim-io` GitHub org profile so search engines can resolve the "Moadim" entity to a verifiable external profile — distinct from `SoftwareApplication.sameAs`, which links the product's own distribution channels (repo, crates.io).
