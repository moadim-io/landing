---
"landing": patch
---

chore(deps-dev): bump eslint to 9.39.5

Refreshes `package-lock.json` to pick up the latest patch release within the existing `^9` range in `package.json`. No config or rule-set changes; `npm run lint` and the full test suite pass unchanged.
