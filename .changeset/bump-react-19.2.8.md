---
"landing": patch
---

Bump `react`/`react-dom` from `19.2.7` to `19.2.8` (patch release, no breaking changes). Keeps the two packages in lockstep, matching `react-dom`'s `peerDependencies.react` constraint, which the published `19.2.8` release also bumped to `^19.2.8`.
