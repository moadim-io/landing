---
"landing": patch
---

Add the "back to homepage" link that `app/error.tsx`'s own copy already promised ("You can try again, or head back to the homepage") but never rendered — only the `Try again` retry button existed. Mirrors the `Back to home` `Link` already used in the sibling `not-found.tsx` route.
