---
"landing": patch
---

Bump `@vitest/coverage-v8` from `^4.1.9` to `^4.1.10` to stay in lockstep with the installed `vitest@4.1.10` (matching major.minor.patch is required for the coverage provider to load). Dev-only tooling dependency; `npm run test:coverage` output is unchanged.
