---
"landing": patch
---

Add a "Not cron. Not CI." comparison section to the homepage, positioning a loop against the two things visitors reach for first — cron and CI — across trigger, what runs, isolation, and where it runs (#186). The page previously explained what a loop is but never said what it isn't.
